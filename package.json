{
  "name": "@tspower/reflection",
  "version": "1.1.13",
  "description": "Easy javascript object reflection",
  "main": "./lib/reflection.js",
  "types": "./lib/reflection.d.ts",
  "repository": {
    "url": "https://github.com/TsPowerOne/Reflection"
  },
  "scripts": {
    "setup": "npm install",
    "build": "tsc",
    "build-watch": "tsc --watch",
    "test-only": "mocha --require ts-node/register test/**/*.ts",
    "pretest": "npm run build",
    "test": "npm run test-only",
    "typedoc": "typedoc --out ./docs --mode modules --tsconfig ./tsconfig.json "
  },
  "author": "Luigi Senzamici (Pegaso Professional)",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/chai": "^4.1.6",
    "@types/mocha": "^5.2.5",
    "chai": "^4.2.0",
    "mocha": "^5.2.0",
    "npm-run-all": "^4.1.3",
    "shx": "^0.3.2",
    "ts-node": "^7.0.1",
    "typedoc": "^0.12.0",
    "typescript": "^3.1.1"
  },
  "directories": {
    "lib": "dist"
  },
  "keywords": [
    "javscript",
    "object",
    "reflection"
  ],
  "publishConfig": {
    "access": "public"
  }
}
